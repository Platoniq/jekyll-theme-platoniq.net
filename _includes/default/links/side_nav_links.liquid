{%- comment -%} 
This component defines the layout of the links in the side menu. It accepts the following parameters: 
- links: name of the file where links are defined.
- header: if this parameter is present, the list is diplayed as header used for the different sizes of the links on the menu.
{%- endcomment -%}

{%- assign links = include.links -%}
{%- assign locale = page.locale -%}

<div class="pt-3 pb-3">
    <ul class="navbar-nav justify-content-end flex-grow-1">
        {%- for link in site.data[locale].[links] -%}
        <li class="nav-item ">
            <a class="nav-link side-nav-item" aria-current="page" href="{{ link.link }}" aria-label="{{ link.name }}">
                {% if include.header%}
                    <h6>{{ link.name }}</h6>
                {% endif %}
            </a>
        </li>
        {%- endfor -%}
    </ul>
</div>